#pragma once

namespace fe { namespace morphengine {

// Configuration for mapping EMU layer filter parameters to DSP domain
struct EmuMapConfig {
    // Placeholder for EMU filter parameter mapping configuration
    // Currently using default mapping behavior
};

}} // namespace fe::morphengine